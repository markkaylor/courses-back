"use strict";
const { parseMultipartData, sanitizeEntity } = require("strapi-utils");

/**
 * Read the documentation (https://strapi.io/documentation/v3.x/concepts/controllers.html#core-controllers)
 * to customize this controller
 */

module.exports = {
    // on find

    // store on course the ytVideoCount
    // if (ytVideoCount !== course.lessons.length)
    // call youtube
    // for lesson in course.lessons
    // create a lesson for all ytTitles not found in course.lessons
    // update a lesson for all lessons that do not have a url or url does not match yt counterpart
    // delete a lesson if lesson.title is not found in ytTitle
  
};
